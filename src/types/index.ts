export type Students = {
  students: StudentProfile[];
};

export type StudentProfile = {
  city: string;
  company: string;
  email: string;
  firstName: string;
  grades: string[];
  id: '1';
  lastName: string;
  pic: string;
  skill: string;
  tags: string[];
};
